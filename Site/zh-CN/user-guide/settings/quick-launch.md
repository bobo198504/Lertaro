# 快速启动设置

快速启动是快速搜索窗口在搜索框为空时显示的启动面板。它会组合手动管理的项目，以及收藏夹、历史记录等已选中的动态数据源。

## 1. 总开关

- **启用快速启动面板**：启用该面板。只有在快速搜索窗口的搜索框为空，且至少有一个数据源存在可用数据时，面板才会显示。
- **显示快捷键徽章**：显示每个可见项目对应的键盘快捷键。该选项默认开启；隐藏徽章不会禁用快捷键。
- 关闭后，面板不会加载任何数据源。
- 面板高度会根据项目数量最多的数据源计算，同时不会超过快速搜索窗口结果区域的最大高度。

## 2. 手动快速启动项目

打开**设置 → 快速启动 → 快速启动**，即可管理自己的项目：

- 可以添加文件、文件夹、网址或 Windows Shell 路径，检查目标时支持展开环境变量。
- 显示名称可以留空，留空时 Lertaro 会根据目标自动生成名称。
- 文件和文件夹浏览按钮支持一次选择多个项目，每个有效且不重复的目标都会作为独立项目添加。
- 使用拖拽手柄调整顺序。点击编辑后，当前行会变成带有保存和取消按钮的就地编辑器；删除按钮用于移除项目。
- 对于手动项目，将鼠标悬停在卡片上并点击省略号按钮即可打开菜单，然后选择**编辑**或**删除**；**编辑**会打开项目编辑对话框。
- 列表有内容时，可以使用列表标题右侧的**清空列表**按钮一次移除所有手动项目；列表为空时该按钮会隐藏。
- 当前不可用的目标会从面板中隐藏，目标恢复可用后会自动重新出现。

## 3. 数据源

打开**设置 → 快速启动 → 数据源**，即可选择动态数据源：

- 数据源来自已安装的快速面板标签页提供程序，因此快速启动可以直接复用收藏夹、历史记录、Windows 历史记录、上次目录、最近文件以及后续插件数据源，不需要维护第二份数据。
- 数据源提供程序存在时默认启用，用户设置中只保存明确关闭的数据源。
- 只有已启用且当前能返回数据的数据源才会创建面板标签页，没有数据的源会隐藏。
- 可以通过数据源旁的拖拽手柄调整面板标签页顺序，该顺序会随快速启动设置保存。
- 当所有手动项目和动态数据源都没有数据时，快速启动面板会隐藏。

## 4. 面板交互

- 面板列数会根据搜索栏宽度自适应。
- 只有一个数据源时，底部数据源指示器会隐藏。
- 有多个数据源时，底部会为每个数据源显示一个圆点。选中的圆点为蓝色，未选中的圆点为灰色；鼠标悬停时圆点会展开显示数据源名称。
- 在面板上按住 **Shift** 滚动鼠标滚轮可以循环切换数据源，切换后的数据源会短暂播放同样的展开动画。
- 配置的**选中下一项**和**选中上一项**快捷键（默认是 **Ctrl+N** 和 **Ctrl+P**）在面板显示时也可以循环切换数据源，到达末尾后会回到第一个数据源，反向切换时同理。
- 每个可见项目都可以使用配置的**跳转选择**修饰键加 **1–9**、**0** 或 **A–Z** 打开。第一个项目使用 `1`，第十个项目使用 `0`，后续项目使用 `A–Z`，因此最多可以为 36 个可见项目提供快捷键。
- 面板显示时，可以使用方向键按照项目的视觉网格移动选择：**←** 和 **→** 会跨越行边界继续移动，**↑** 和 **↓** 会在相邻方向寻找最近的同列项目，也会跨越分组；只有相邻方向中整列都没有项目时才保持当前位置，项目选择不会循环。
- 右键单击任意数据源中的项目，会在快速启动面板内部打开该项目的悬浮动作菜单。面板会临时扩展到动作列表的最大工作高度，关闭菜单后恢复原来的高度；点击菜单之外的区域即可关闭菜单。
- 当面板中的项目超出可见高度时，可以在项目区域使用鼠标滚轮按整行滚动；鼠标悬停在项目上不会阻止滚动。快捷键徽章及其对应项目会随着可见行一起递进。
- 开始输入搜索内容后，面板会隐藏。
